{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "build:utils": "esbuild utils/criticalityV2.js --bundle --platform=node --format=cjs --outfile=utils/criticality_dist.js && esbuild utils/erosionCoordinates.js --bundle --platform=node --format=cjs --outfile=utils/erosionCoordinates_dist.js && esbuild utils/erosionUtils.js --bundle --platform=node --format=cjs --outfile=utils/erosionUtils_dist.js && esbuild utils/statusUtils.js --bundle --platform=node --format=cjs --outfile=utils/statusUtils_dist.js",
    "prestart": "npm run build:utils",
    "predev": "npm run build:utils",
    "pretest": "npm run build:utils"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "jest": "^30.2.0",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
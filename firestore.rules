rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access now goes through the backend API (Admin SDK bypasses rules).
    // Client-side Firestore access is no longer needed after auth migration.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
